version: '3.4'

services:
  naming-server:
    image: rodrigolucio/naming-server:0.0.1-SNAPSHOT
    ports:
      - 8761:8761
    networks:
      - rodrigo-network
networks:
  rodrigo-network:
    driver: bridge
