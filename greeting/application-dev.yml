spring:
  application:
    name: greeting

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'

greeting-service:
  greeting: "Dev"
  default-value: "Você esta no profile dev"


