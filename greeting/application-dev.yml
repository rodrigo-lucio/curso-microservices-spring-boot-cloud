spring:
  application:
    name: greeting

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'

greeting-service:
  greeting: "Teste alt"
  default-value: "Valor default dev alt"


